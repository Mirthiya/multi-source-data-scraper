# Multi-Source Data Scraping Pipeline v2
# ========================================
# Install: pip install -r requirements.txt

# ── Async HTTP ──────────────────────────────────────
httpx>=0.27.0          # async HTTP client (replaces requests)
beautifulsoup4>=4.12   # HTML parsing
lxml>=5.0              # fast BS4 parser backend

# ── YouTube ─────────────────────────────────────────
youtube-transcript-api>=0.6.2  # transcript extraction

# ── NLP & Topic Tagging ─────────────────────────────
keybert>=0.8.3                 # BERT-based keyword extraction (main)
sentence-transformers>=2.7.0   # backend for KeyBERT (all-MiniLM-L6-v2)
scikit-learn>=1.4.0            # TF-IDF fallback + cosine similarity dedup
langdetect>=1.0.9              # language detection

# ── Evaluation & Explainability ──────────────────────
shap>=0.45.0                   # SHAP explainability (optional, for advanced report)
numpy>=1.26.0
pandas>=2.2.0                  # data manipulation

# ── Utilities ────────────────────────────────────────
python-dateutil>=2.9.0         # robust date parsing
tqdm>=4.66.0                   # progress bars
